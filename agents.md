# Git Working Rules

- This folder is a self-contained git repository.
- Work directly on `main` unless a different branch is explicitly requested.
- Push completed work to `origin/main`.
- Use one-line high-signal commit messages with conventional prefixes:
  - `feat:`
  - `fix:`
  - `docs:`
  - `chore:`
  - `refactor:`
  - `test:`
- Keep commit messages concise and professional.
- Never add `Co-authored-by`, `Made-with: Cursor`, AI attribution, or any note saying the commit was generated by an assistant.
- Commit messages must be a single line title only (no trailers/footers).
- Enforce trailer stripping with local hook: `.git/hooks/commit-msg` removes `Made-with: Cursor`.
- Before pushing, verify with:
  - `git log -20 --grep="Made-with: Cursor" --pretty=oneline`
  - `git log -1 --pretty=full`
- Prefer multiple small logical commits over one large commit.
- Commit implementation, docs, and experiment assets separately when possible.
- Do not commit files that are intentionally ignored by `.gitignore`.
- Avoid destructive git commands unless explicitly requested.

# FAAR Agent Operating Notes

## Stack Choices

Use the recommended column from the fixed experimental plan unless the user explicitly says compute is constrained.

| Component | Selected Stack |
| --- | --- |
| VLM | `gpt-4o-2024-11-20` (user-approved Alternate A) |
| Embeddings | `NV-Embed-v2` |
| Reranker | `BGE-reranker-v2-m3` |
| OCR | `GOT-OCR 2.0` |
| PDF Pre-processing | `Docling` |
| Visual RAG Base | `ColPali` |
| Eval Framework | `RAGAS` |
| Experiment Track | `Weights & Biases` |
| Annotation Tool | `Label Studio` |

## Model and Tool Version Pins

Put these in the paper implementation section.

| Role | Pin |
| --- | --- |
| VLM | `gpt-4o-2024-11-20` |
| Embeddings | `NV-Embed-v2` |
| Reranker | `bge-reranker-v2-m3` |
| OCR | `GOT-OCR 2.0` |
| PDF Prep | `Docling 1.x` |
| Eval | `RAGAS 0.2.x` |
| Tracking | `wandb 0.17.x` |
| Annotation | `Label Studio 1.x` |

## Non-Negotiables

- `split.json` is created once and never touched again.
- The gate threshold is tuned on validation data only, never on test data.
- `evaluate.py` must always emit `EM`, `F1`, `vlm_rate`, and `harm_rate`, or the run does not count.
- Never commit API keys or credentials.
- Do not silently switch to the free/local stack when a required paid key is missing.
- Keep all work on the `faar-aaai-experiments` branch until human review and merge.
